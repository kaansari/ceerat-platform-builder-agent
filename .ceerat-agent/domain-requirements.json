{
  "domains": {
    "commerce": {
      "must_have_fields": [
        {
          "name": "customers.shipping_and_billing_addresses",
          "requirement": "Customers store distinct profile, shipping, and billing addresses. Complete shipping and billing addresses require line1, city, state, country, and postal_code.",
          "implementation_hint": "Expose shipping_address and billing_address through customer.Customer and UpdateMyCustomerProfile. Customer self-service identity comes from JWT context."
        },
        {
          "name": "orders.address_and_pricing_snapshot",
          "requirement": "Orders snapshot shipping address, billing address, subtotal, discount, shipping, tax, total, selected shipping method, coupon code/label, tax rate, and tax label.",
          "implementation_hint": "Copy explicit customer shipping/billing addresses into orders during creation. Repricing must use the order shipping snapshot and reject missing snapshots."
        },
        {
          "name": "order_pricing_rules",
          "requirement": "Tax, shipping, and coupon behavior uses OrderPricingRule with kind, calculation, value, minimum subtotal, region, active window, priority, and optional code.",
          "implementation_hint": "Use kind tax, shipping, or discount. Fixed and percentage calculations are supported; discount codes are normalized and matched case-insensitively."
        }
      ],
      "must_have_relationships": [
        {
          "name": "customer_addresses_to_order",
          "requirement": "Checkout resolves the authenticated customer, requires explicit shipping and billing addresses, and snapshots both onto the order.",
          "implementation_hint": "Do not accept customer_id, tax region, shipping address, billing address, line prices, or totals as trusted browser checkout authority."
        },
        {
          "name": "cart_to_order_pricing",
          "requirement": "Cart quote and checkout use the same server-owned pricing calculator after active catalog item discounts are applied.",
          "implementation_hint": "Calculate subtotal - order discount + shipping + tax and round monetary values to cents. Checkout remains idempotent and transactional."
        }
      ],
      "business_rules": [
        {
          "name": "shipping_address_tax_jurisdiction",
          "requirement": "Tax jurisdiction is determined strictly from the order/customer shipping address, never profile or billing address.",
          "implementation_hint": "Choose the most specific active state/country tax rule. Use 9 percent only when no configured rule matches."
        },
        {
          "name": "default_shipping_methods",
          "requirement": "Checkout offers Free at 0, Standard at 5, Three day at 10, and Next day at 20 unless a matching configured rule with the same name replaces that option.",
          "implementation_hint": "Use stable default shipping IDs. Reject stale or unavailable IDs; do not remap legacy selections."
        },
        {
          "name": "coupon_selection",
          "requirement": "A coupon is an order discount rule with a non-empty code. It may be scheduled, regional, priority-based, and subject to a minimum subtotal.",
          "implementation_hint": "Apply at most one order discount. Explicit valid code wins the coupon path; automatic discount rules have no code. Never trust a browser-calculated discount."
        },
        {
          "name": "strict_new_system_state",
          "requirement": "Commerce state has no backward-compatibility address or shipping-selection fallback.",
          "implementation_hint": "Reject incomplete customer addresses, orders without address snapshots, and stale shipping IDs instead of falling back to profile/current-customer state."
        }
      ],
      "workflow_rules": [
        {
          "name": "customer_checkout",
          "requirement": "Customer product checkout quotes eligible shipping, coupon, tax, and total through order.OrderManager and displays shipping/billing address review before order creation.",
          "implementation_hint": "Use customer UI same-origin endpoints backed by QuoteMyCartPricing and CheckoutMyCart. Address management belongs under customer preferences."
        },
        {
          "name": "agent_order_pricing",
          "requirement": "Agents manage tax, shipping, and coupon rules and may reprice user-scoped orders.",
          "implementation_hint": "Use Create/List/Update/DeleteOrderPricingRule and RepriceOrder. Customer role receives only QuoteMyCartPricing and CheckoutMyCart access."
        }
      ]
    },
    "career": {
      "must_have_fields": [
        {
          "name": "jobs.source_external_identity",
          "requirement": "Imported ATS jobs must preserve source, source_url, and external_job_id for idempotency, provider detail lookup, and application fallback.",
          "implementation_hint": "Keep source/source_url/external_job_id on jobs and source/external_id/source_url on companies. Use company_id + source + external_job_id as the natural imported job identity."
        },
        {
          "name": "employment_records.customer_id",
          "requirement": "Employment records are reusable customer-owned Career records, not skills and not resume-only text.",
          "implementation_hint": "Scope employment_records by authenticated customer id and attach to resumes through resume_employment_records."
        },
        {
          "name": "job_application_submissions.sanitized_provider_audit",
          "requirement": "External provider submissions must retain sanitized audit status without storing raw sensitive provider payloads.",
          "implementation_hint": "Store provider name/status/application URL and sanitized failure/status metadata; do not store raw response bodies, tokens, or resume binary data."
        }
      ],
      "must_have_relationships": [
        {
          "name": "customer_to_career_records",
          "requirement": "Customer-owned skill profiles, resumes, employment records, carts, and applications must resolve ownership from authenticated JWT context.",
          "implementation_hint": "Handlers look up customers.user_id and repositories scope by customer_id. Do not trust request-supplied customer_id for customer self-service."
        },
        {
          "name": "resume_to_employment_records",
          "requirement": "A resume can attach multiple reusable employment records, and one employment record can be reused across resumes.",
          "implementation_hint": "Use resume_employment_records with sort_order, include, tailored_title, and tailored_summary. Verify ownership of both sides before mutation."
        },
        {
          "name": "resume_text_import",
          "requirement": "Resume text upload/import must parse and persist through CareerProfile service APIs, not through direct app, AI, or database writes.",
          "implementation_hint": "Use ParseResumeText for .txt/.md validation and draft extraction, then ImportResumeDraft or batch CareerProfile APIs to create skill profile, skills, employment records, resume, and attachments under authenticated customer ownership."
        },
        {
          "name": "job_to_application_submission",
          "requirement": "Internal applications and external ATS provider submissions are separate records.",
          "implementation_hint": "Preserve job_applications as the internal application record and job_application_submissions as sanitized provider submission/audit records."
        }
      ],
      "business_rules": [
        {
          "name": "service_owned_search",
          "requirement": "Career job search is owned by career.JobService/SearchJobs and may use Typesense only behind ceerat-user-service.",
          "implementation_hint": "Browser apps, crawlers, and AI tools call Ceerat APIs/gRPC only. Keep Postgres as source of truth and hide Typesense API keys from frontends."
        },
        {
          "name": "service_owned_metrics",
          "requirement": "Career market and customer workflow KPIs are service-owned read models, not app-side broad count queries.",
          "implementation_hint": "Use GetCareerMarketMetrics and GetMyCareerMetrics; refresh market metrics after safe ATS import where practical."
        },
        {
          "name": "external_application_consent",
          "requirement": "External ATS submissions require discovery, explicit customer confirmation, supported required fields, and manual fallback when unsafe.",
          "implementation_hint": "Never let UI, crawlers, or AI submit directly to providers. Do not invent answers to required provider questions."
        }
      ],
      "workflow_rules": [
        {
          "name": "customer_career_navigation",
          "requirement": "Customer Career pages use separate overview, list/search, create, detail, and edit surfaces with consistent navigation and breadcrumbs.",
          "implementation_hint": "Use /customer/career/{entity}, /new, /{id}, and /{id}/edit routes where practical. Detail pages should not render list/search below them."
        },
        {
          "name": "resume_export",
          "requirement": "Resume view/download should include profile content, profile skills, and attached employment records in readable full-page output.",
          "implementation_hint": "Avoid raw pipe separators and do not expose raw PDF bytes in chat or page text."
        },
        {
          "name": "ai_job_search_limits",
          "requirement": "AI search_jobs returns compact capped summaries and uses get_job for full detail.",
          "implementation_hint": "Default AI job search to 10 results, cap at 20, exclude full descriptions from search result payloads, show posting date and More info source_url links when available, do not display internal GUID/job_id values, and preserve number-to-job_id mappings in hidden ceerat_job_refs metadata so follow-up details call get_job with the stable Ceerat id."
        }
      ]
    },
    "invoice": {
      "must_have_fields": [
        {
          "name": "order_id",
          "requirement": "Invoice must identify the order it bills.",
          "implementation_hint": "Add invoices.order_id as a required foreign key to orders.id and expose order_id in create/get/list invoice contract messages."
        }
      ],
      "must_have_relationships": [
        {
          "name": "order_to_invoices",
          "requirement": "One order can have many invoices; each invoice belongs to one order.",
          "implementation_hint": "Use invoices.order_id -> orders.id. Add an index on invoices.order_id. Do not add a unique constraint on order_id."
        }
      ],
      "business_rules": [],
      "workflow_rules": []
    }
  }
}
