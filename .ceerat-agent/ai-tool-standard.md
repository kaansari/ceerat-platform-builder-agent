# AI Tool Standard

AI agent tools should be explicit, narrow, permission-checked, and auditable.
Read tools and mutation tools should be separate. Plans should include tool
names, the data each tool accepts, the data each tool returns, and the
permissions required to invoke it.
